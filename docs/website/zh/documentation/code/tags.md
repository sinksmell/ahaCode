---
title: 代码标签
description: "在 ahaCode 中使用标签跨文件夹组织代码片段。"
---

# 标签

标签是跨文件夹组织代码片段的灵活方式。与文件夹不同，一个代码片段可以同时拥有多个标签。

## 添加标签

选择代码片段，然后在片段名称下方的 **"添加标签"** 字段中输入标签名称。

::: info
如果标签已存在，它会出现在下拉列表中供你选择。标签是唯一的，不能重复。
:::

## 删除标签

右键点击标签，选择 **"删除"**。

::: warning
删除标签时，它也会从所有使用该标签的代码片段中移除。
:::
